import assert from 'assert';
import Life from './src/Life.js';

describe('Life', function () {
    it('should ...', function () {

    });
    it('should ...', function () {

    });
});


// example
/*
describe('willBeAlive', function () {
    it('should be dead if <2', function () {
        const neighbours = 1;
        const state = true;
        assert.equal(willBeAlive(neighbours, state), false);
    });
});
*/
